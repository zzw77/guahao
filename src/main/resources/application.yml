spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guahao?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: lanxin

server:
  port: 8888

mybatis-plus:
  mapper-locations: classpath:mappers/*.xml
  config-location: classpath:mybatisconfig.xml

servlet:
  multipart:
    max-file-size: 5Mb
    max-request-size: 50Mb
